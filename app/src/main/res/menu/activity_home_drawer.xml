<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_latest_daily"
            android:icon="@drawable/ic_menu_latest"
            android:title="@string/latest_daily"/>
        <item
            android:id="@+id/nav_history_daily"
            android:icon="@drawable/ic_menu_history"
            android:title="@string/history_daily"/>
    </group>

    <item android:title="@string/nav_group_other">
        <menu>
            <item
                android:id="@+id/nav_pic_mode"
                android:icon="@drawable/ic_menu_picture"
                android:title="@string/no_pic_mode"
                app:actionLayout="@layout/action_view_switch"/>
            <item
                android:id="@+id/nav_skin"
                android:icon="@drawable/ic_menu_skin"
                android:title="@string/skin"/>
            <item
                android:id="@+id/nav_about"
                android:icon="@drawable/ic_menu_about"
                android:title="@string/about"/>
        </menu>
    </item>

</menu>
